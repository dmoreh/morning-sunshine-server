<%= form_with(model: document, local: true) do |form| %>
  <% if document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_field :filename, id: :document_filename %>
  </div>

  <div class="field">
    <%= form.label :content_type %>
    <%= form.text_field :content_type, id: :document_content_type %>
  </div>

  <div class="field">
    <%= form.label :file_contents %>
    <%= form.text_field :file_contents, id: :document_file_contents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
